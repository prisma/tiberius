[package]
name = "tvp-macro"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Derive macro for tiberius table-valued parameter rows."
repository = "https://github.com/prisma/tiberius"

[dependencies]
syn = "^2"
quote = "^1"
proc-macro2 = "^1"

[lib]
proc-macro = true
